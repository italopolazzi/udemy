.card {
  margin: 0.5 * dimentions.default;
  padding: dimentions.default;
  background: theme[tt].body_color_background;
  color: theme[tt].body_color;
  box-shadow: defaults.box_shadow;
  width: 100%;
}

.card-title {
  margin-bottom: dimentions.default;
  font-weight: bold;
}
